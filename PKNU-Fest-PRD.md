# PKNU-Fest PRD (Product Requirements Document)

**문서 버전**: 1.1  
**작성일**: 2025년 12월  
**최종 수정**: 2025년 12월
**프로젝트명**: PKNU-Fest - 부경대학교 축제 통합 플랫폼

---

## 📋 목차

1. [제품 개요](#1-제품-개요)
2. [비전 및 목표](#2-비전-및-목표)
3. [사용자 페르소나](#3-사용자-페르소나)
4. [핵심 기능 요구사항](#4-핵심-기능-요구사항)
5. [사용자 플로우](#5-사용자-플로우)
6. [비기능 요구사항](#6-비기능-요구사항)
7. [기술 스택 및 아키텍처](#7-기술-스택-및-아키텍처)
8. [우선순위 및 로드맵](#8-우선순위-및-로드맵)
9. [성공 지표 (KPI)](#9-성공-지표-kpi)
10. [리스크 및 제약사항](#10-리스크-및-제약사항)
11. [향후 확장 가능성](#11-향후-확장-가능성)

---

## 1. 제품 개요

### 1.1 배경

부경대학교 축제는 매년 수천 명의 학생과 방문객이 참여하는 대규모 행사입니다. 하지만 현재는:

- 행사 정보가 분산되어 있어 찾기 어려움
- 예약 시스템이 없어 인기 프로그램에 참여하기 어려움
- 푸드트럭 주문 시 긴 대기 시간 발생
- 실시간 정보 업데이트 부재로 혼란 발생

### 1.2 제품 설명

PKNU-Fest는 축제 참여자, 운영진, 푸드트럭 운영자를 연결하는 통합 웹 플랫폼입니다. GPS 기반 지도, 실시간 예약, 스마트 오더 기능을 통해 축제 경험을 혁신합니다.

### 1.3 핵심 가치 제안

**참여자**

- 한 곳에서 모든 축제 정보 확인
- 미리 예약하여 대기 시간 절약
- 푸드트럭 사전 주문으로 편리한 식사

**운영진**

- 효율적인 행사 관리
- 실시간 현황 모니터링
- 데이터 기반 의사결정

**푸드트럭 운영자**

- 주문 자동화로 업무 효율 향상
- 매출 증대 기회
- 간편한 메뉴 관리

---

## 4. 핵심 기능 요구사항

### 4.1 인증 및 사용자 관리

#### 4.1.1 회원가입 및 로그인

**필수 기능**

- 이메일 회원가입 (일반 이메일 가능)
- 일반 로그인 (이메일 + 비밀번호)
- 소셜 로그인 (카카오, 구글)
- 비밀번호 찾기 / 재설정
- 자동 로그인 유지 (Remember Me)

**사용자 역할**

- 일반 참여자 (기본)
- 운영진 (관리자 권한)
- 푸드트럭 운영자 (판매자 권한)

**입력 필드**

- 이메일 (필수)
- 비밀번호 (필수, 8자 이상, 영문+숫자+특수문자)
- 이름 (필수)
- 전화번호 (필수, 주문 알림용)
  - 📌 전화번호 인증은 Phase 3 이후 구현

**검증 규칙**

- 중복 이메일 체크
- 비밀번호 강도 검사
- 전화번호 형식 검증

**학생 인증 (선택적)**

- 회원가입 직후에는 일반 사용자로 등록
- 마이페이지에서 선택적으로 학생 인증 가능
- 부경대 이메일 (@pknu.ac.kr)로 인증 코드 발송
- 인증 완료 시 `isStudentVerified` 플래그 활성화
- **학생 전용 행사 예약 시 필수**

#### 4.1.2 프로필 관리

- 프로필 정보 수정
- **학생 인증** (부경대 이메일 인증)
  - 인증 상태 표시
  - 인증하기 버튼
  - 인증 완료 뱃지
- 알림 설정 (푸시, 이메일)
- 비밀번호 변경
- 회원 탈퇴

### 4.2 행사 관리 시스템

#### 4.2.1 행사 등록 (운영진 전용)

**필수 정보**

- 행사명
- 카테고리 (공연, 체험, 전시, 게임 등)
- 설명 (최소 10자, 최대 500자)
- 날짜 및 시간
- 장소 (GPS 좌표 포함)
- 썸네일 이미지
- 예약 필요 여부

**선택 정보**

- 상세 이미지 (최대 5장)
- 주최 단체
- 연령 제한
- 준비물
- 주의사항
- 외부 링크 (인스타그램, 유튜브 등)

**📌 NEW: 학생 전용 설정**

- [ ] 학생 전용 행사
  - 체크 시: 학생 인증 완료자만 예약 가능
  - 미인증 사용자에게는 "🎓 학생 전용" 뱃지 표시
  - 예약 버튼 호버 시 "인증하러 가기"로 변경
  - 클릭 시 마이페이지 학생 인증으로 이동

**📌 NEW: 인원 제한 설정**

- [ ] 인원 제한 없음
  - 체크 시: 자유 참여 (선착순 아님)
  - 예: 야외 공연, 전시
- [ ✓ ] 최대 인원 설정
  - 최대 인원: [100]명
  - 예: 강연, 체험 부스

**시간대 설정**

- 단일 시간대 또는 다중 시간대 선택
- 시간대별 참여 인원 설정
- 시간대별 예약 마감 설정

**상태 관리**

- 대기 (등록 완료, 아직 공개 안됨)
- 공개 (참여자에게 노출)
- 예약 마감 (정원 도달)
- 진행 중
- 종료

#### 4.2.2 행사 검색 및 필터링 (참여자)

**검색 기능**

- 키워드 검색 (행사명, 설명, 주최 단체)
- 실시간 자동완성

**필터 옵션**

- 카테고리
- 날짜
- 시간대
- 장소
- 예약 가능 여부
- 무료/유료
- **🎓 학생 전용 포함/제외**

**정렬 옵션**

- 인기순 (예약 많은 순)
- 최신순
- 시간 임박순
- 거리순 (현재 위치 기준)

#### 4.2.3 행사 상세 페이지

**표시 정보**

- 행사 기본 정보 (이름, 카테고리, 시간, 장소)
- **🎓 학생 전용 뱃지** (해당 시)
- 이미지 갤러리 (스와이프 가능)
- 상세 설명
- 지도에서 위치 표시
- 예약 가능 시간대 목록 (인원 제한 있을 경우)
- 실시간 잔여 좌석 (인원 제한 있을 경우)
- 주최 단체 정보
- 관련 행사 추천

**인터랙션**

- 찜하기 (북마크)
- 공유하기 (카카오톡, URL 복사)
- **예약하기 버튼**
  - 학생 전용 행사 + 미인증 사용자: "인증하러 가기" 버튼으로 변경
  - 인원 제한 없음: "참여 의사 표시" 또는 버튼 없음
- 길찾기 버튼 (지도 앱 연동)

### 4.3 예약 시스템

#### 4.3.1 예약 생성

**예약 프로세스**

1. 행사 선택
2. **학생 전용 확인** (학생 전용 행사일 경우 인증 여부 체크)
3. 시간대 선택 (인원 제한 있을 경우)
4. 참여 인원 선택 (1-4명, 인원 제한 있을 경우)
5. 정보 확인
6. 예약 완료

**예약 제한**

- 사용자당 동일 행사 1회 예약
- 시간대별 정원 제한 (인원 제한 있을 경우)
- 중복 시간대 예약 방지
- **학생 전용 행사는 학생 인증 필수**

**예약 확인**

- 예약 번호 발급 (8자리)
- QR 코드 생성
- 예약 상세 정보 표시

#### 4.3.2 예약 관리

**내 예약 목록**

- 진행 예정 예약
- 완료된 예약
- 취소된 예약

**예약 상태**

- 예약 완료 (Confirmed)
- 체크인 완료 (Checked In)
- 노쇼 (No Show)
- 취소 (Cancelled)

**예약 취소**

- 행사 시작 1시간 전까지 취소 가능
- 취소 확인 모달
- 취소 완료 알림

#### 4.3.3 QR 체크인

**참여자 측**

- 예약 상세에서 QR 코드 표시
- 전체 화면 모드
- 화면 밝기 자동 조절
- 오프라인에서도 QR 표시

**운영진 측**

- QR 스캐너 화면
- 카메라 권한 요청
- 스캔 성공/실패 피드백
- 중복 체크인 방지
- 수동 확인 옵션 (예약번호 입력)

### 4.4 지도 기능

#### 4.4.1 캠퍼스 지도

**지도 표시**

- Kakao Map API 사용
- 부경대 캠퍼스 행사 진행 위치를 중심으로 표시
- 확대/축소 컨트롤
- 현재 위치 표시
- 나침반 (방향)

**마커 표시**

- 행사 장소 (카테고리별 색상)
- 푸드트럭 위치
- 보건실

**마커 인터랙션**

- 마커 클릭 시 요약 정보 표시
- 상세 페이지로 이동 버튼
- 길찾기 버튼

#### 4.4.2 실시간 위치 추적

- 사용자 현재 위치 표시
- 위치 권한 요청
- 백그라운드 위치 추적 (선택)

#### 4.4.3 길찾기

- 현재 위치에서 목적지까지 경로 표시
- 도보 소요 시간 계산
- 외부 지도 앱 연동 (카카오맵, 네이버지도)

### 4.5 푸드트럭 및 주문 시스템

#### 4.5.1 푸드트럭 목록

**목록 표시**

- 카드 형식 레이아웃
- 썸네일 이미지
- 푸드트럭 이름
- 카테고리 (한식, 양식, 디저트, 음료 등)
- 실시간 운영 상태 (영업 중/마감/품절)

**필터 및 정렬**

- 카테고리 필터
- 인기순 정렬

#### 4.5.2 푸드트럭 상세 및 메뉴

**푸드트럭 정보**

- 대표 이미지
- 소개
- 운영 시간
- 위치 (지도)
- 연락처

**메뉴 표시**

- 이미지
- 메뉴명
- 설명
- 가격
- 품절 여부
- 인기 메뉴 뱃지 (대표 메뉴 뱃지)
- 알레르기 정보

**주문 담기**

- 메뉴 및 수량 선택
- 옵션 선택 (매운 정도, 추가 토핑 등)
- 요청사항 입력
- 장바구니에 추가

#### 4.5.3 주문 및 결제

**장바구니**

- 주문 아이템 목록
- 수량 변경
- 아이템 삭제
- 총 금액 계산
- 할인/쿠폰 적용

**주문 정보 입력**

- 전화번호 확인
- 요청사항 (선택)
- 포장/매장 선택

**결제**

- 토스페이먼츠 연동
- 결제 수단 선택 (카드, 간편결제)
- 결제 진행
- 결제 완료 확인

**주문 완료**

- 주문번호 발급 (주문 순서대로 1번부터)
- 주문 상세 정보
- 예상 대기 시간
- 푸드트럭 위치 지도
- 실시간 주문 상태 추적

#### 4.5.4 주문 상태 추적

**주문 상태**

- 주문 접수 (Order Received)
- 조리 중 (Preparing)
- 준비 완료 (Ready for Pickup)
- 픽업 완료 (Completed)
- 취소 (Cancelled) -> 조리중에는 취소 불가

**알림**

- 주문 접수 시
- 조리 시작 시
- 준비 완료 시 (푸시 알림)

**내 주문 내역**

- 진행 중인 주문
- 완료된 주문
- 취소된 주문

#### 4.5.5 푸드트럭 운영자 기능

**주문 관리 대시보드**

- 실시간 주문 목록
- 주문 상태별 필터링
- 주문 상세 보기
- 주문 상태 변경
- 주문 취소 (환불 처리)

**메뉴 관리**

- 메뉴 추가/수정/삭제
- 이미지 업로드
- 가격 설정
- 품절 처리
- 옵션 설정

**운영 관리**

- 영업 시작/종료
- 임시 휴게
- 메뉴 일괄 품절 처리

**매출 리포트**

- 일별 매출 통계
- 인기 메뉴 순위
- 시간대별 주문량
- 총 주문 건수

### 4.6 알림 시스템

#### 4.6.1 푸시 알림 (Firebase Cloud Messaging)

**자동 알림**

- 예약 확정 시
- 행사 시작 1시간 전
- 행사 변경/취소 시
- 주문 상태 변경 시
- 준비 완료 시 (픽업 가능)

**수동 알림 (운영진)**

- 공지사항 푸시
- 대상 선택 (전체/특정 행사 예약자)
- 제목 및 내용 작성
- 즉시 발송 또는 예약 발송

**알림 설정 (사용자)**

- 푸시 알림 on/off
- 카테고리별 알림 설정
- 방해 금지 시간 설정
- 소리/진동 설정

#### 4.6.2 앱 내 알림

- 알림 목록 페이지
- 읽음/안 읽음 표시
- 알림 클릭 시 해당 페이지로 이동
- 알림 삭제
- 전체 읽음 처리

### 4.7 관리자 대시보드

#### 4.7.1 통계 및 리포트

**실시간 현황**

- 총 등록 사용자 수
- **학생 인증 완료 사용자 수**
- 현재 진행 중인 행사 수
- 오늘 총 예약 건수
- 실시간 활성 사용자 수

**행사 통계**

- 행사별 예약률
- 카테고리별 인기도
- 시간대별 예약 분포
- 노쇼율
- **학생 전용 vs 일반 행사 참여율**

**사용자 통계**

- 일별/주별 신규 가입자
- 활성 사용자 추이
- 사용자별 예약 횟수
- 플랫폼 체류 시간
- **학생 인증률**

**푸드트럭 통계**

- 총 매출액
- 푸드트럭별 매출
- 인기 메뉴 TOP 10
- 시간대별 주문량

#### 4.7.2 행사 관리

- 전체 행사 목록
- 상태별 필터링
- **학생 전용 필터링**
- 행사 검색
- 빠른 수정/삭제
- 일괄 작업 (공개/비공개 전환)

#### 4.7.3 사용자 관리

- 사용자 목록
- 역할별 필터링
- **학생 인증 여부 필터링**
- 사용자 검색
- 사용자 상세 정보
- 역할 변경
- **학생 인증 수동 승인/취소**
- 계정 정지/복구

#### 4.7.4 푸드트럭 관리

- 푸드트럭 목록
- 승인 대기 목록
- 승인/거부 처리
- 푸드트럭 정보 수정
- 강제 영업 종료

#### 4.7.5 공지사항 관리

- 공지사항 작성
- 게시/숨김 처리
- 중요 공지 고정
- 푸시 알림 발송

---

## 7. 기술 스택 및 아키텍처

### 7.3 데이터베이스 스키마 (주요 테이블)

#### Users

```prisma
model User {
  id                 String   @id @default(uuid())
  email              String   @unique
  password           String
  name               String
  phone              String
  role               Role     @default(USER)
  isStudentVerified  Boolean  @default(false)  // 📌 NEW: 학생 인증 여부
  studentEmail       String?  @unique          // 📌 NEW: 인증한 학교 이메일
  verified           Boolean  @default(false)
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt

  reservations Reservation[]
  orders       Order[]
}

enum Role {
  USER
  ADMIN
  VENDOR
}
```

#### Events

```prisma
model Event {
  id            String      @id @default(uuid())
  title         String
  description   String
  category      Category
  thumbnail     String
  images        String[]
  location      String
  latitude      Float
  longitude     Float
  isStudentOnly Boolean     @default(false)  // 📌 NEW: 학생 전용 여부
  capacity      Int?                         // 📌 UPDATED: nullable (인원 제한 없음 가능)
  status        EventStatus @default(DRAFT)
  organizer     String?
  createdAt     DateTime    @default(now())
  updatedAt     DateTime    @updatedAt

  timeSlots    TimeSlot[]
  reservations Reservation[]
}

enum Category {
  PERFORMANCE
  EXPERIENCE
  EXHIBITION
  GAME
  OTHER
}

enum EventStatus {
  DRAFT
  PUBLISHED
  FULL
  ONGOING
  ENDED
}
```

#### TimeSlots

```prisma
model TimeSlot {
  id           String   @id @default(uuid())
  eventId      String
  startTime    DateTime
  endTime      DateTime
  capacity     Int?                          // 📌 UPDATED: nullable
  reserved     Int      @default(0)

  event        Event    @relation(fields: [eventId], references: [id])
  reservations Reservation[]
}
```

#### Reservations

```prisma
model Reservation {
  id           String            @id @default(uuid())
  userId       String
  eventId      String
  timeSlotId   String?                      // 📌 UPDATED: nullable (인원 제한 없을 경우)
  partySize    Int?                         // 📌 UPDATED: nullable
  status       ReservationStatus @default(CONFIRMED)
  qrCode       String            @unique
  checkedInAt  DateTime?
  createdAt    DateTime          @default(now())

  user         User     @relation(fields: [userId], references: [id])
  event        Event    @relation(fields: [eventId], references: [id])
  timeSlot     TimeSlot? @relation(fields: [timeSlotId], references: [id])  // 📌 UPDATED: nullable
}

enum ReservationStatus {
  CONFIRMED
  CHECKED_IN
  NO_SHOW
  CANCELLED
}
```

#### FoodTrucks

```prisma
model FoodTruck {
  id          String   @id @default(uuid())
  name        String
  description String
  category    String
  thumbnail   String
  location    String
  latitude    Float
  longitude   Float
  phone       String
  isOpen      Boolean  @default(false)
  ownerId     String

  owner       User     @relation(fields: [ownerId], references: [id])
  menuItems   MenuItem[]
  orders      Order[]
}
```

#### MenuItems

```prisma
model MenuItem {
  id           String   @id @default(uuid())
  foodTruckId  String
  name         String
  description  String?
  price        Int
  image        String?
  isAvailable  Boolean  @default(true)
  isPopular    Boolean  @default(false)

  foodTruck    FoodTruck @relation(fields: [foodTruckId], references: [id])
  orderItems   OrderItem[]
}
```

#### Orders

```prisma
model Order {
  id           String      @id @default(uuid())
  userId       String
  foodTruckId  String
  pickupNumber String
  totalAmount  Int
  status       OrderStatus @default(RECEIVED)
  paymentId    String?
  createdAt    DateTime    @default(now())

  user         User        @relation(fields: [userId], references: [id])
  foodTruck    FoodTruck   @relation(fields: [foodTruckId], references: [id])
  items        OrderItem[]
}

enum OrderStatus {
  RECEIVED
  PREPARING
  READY
  COMPLETED
  CANCELLED
}
```

#### OrderItems

```prisma
model OrderItem {
  id         String @id @default(uuid())
  orderId    String
  menuItemId String
  quantity   Int
  price      Int
  options    Json?

  order      Order    @relation(fields: [orderId], references: [id])
  menuItem   MenuItem @relation(fields: [menuItemId], references: [id])
}
```

### 7.4 API 설계

#### RESTful API Endpoints

**인증**

- `POST /api/auth/register` - 회원가입 (일반 이메일)
- `POST /api/auth/login` - 로그인
- `POST /api/auth/logout` - 로그아웃
- `POST /api/auth/refresh` - 토큰 갱신
- **`POST /api/auth/verify-student`** - 📌 NEW: 학생 인증 코드 발송
- **`POST /api/auth/confirm-student`** - 📌 NEW: 학생 인증 코드 확인

**사용자**

- `GET /api/users/me` - 내 정보 조회
- `PUT /api/users/me` - 내 정보 수정
- `DELETE /api/users/me` - 회원 탈퇴

**행사**

- `GET /api/events` - 행사 목록 (필터링, 검색, 페이지네이션)
  - **`?studentOnly=true`** - 📌 NEW: 학생 전용만 필터
- `GET /api/events/:id` - 행사 상세
- `POST /api/events` - 행사 등록 (관리자)
- `PUT /api/events/:id` - 행사 수정 (관리자)
- `DELETE /api/events/:id` - 행사 삭제 (관리자)
- `GET /api/events/:id/timeslots` - 시간대 목록

**예약**

- `GET /api/reservations` - 내 예약 목록
- `GET /api/reservations/:id` - 예약 상세
- `POST /api/reservations` - 예약 생성
  - **학생 전용 행사 체크 로직 포함**
- `DELETE /api/reservations/:id` - 예약 취소
- `POST /api/reservations/:id/checkin` - 체크인 (QR 스캔)

**푸드트럭**

- `GET /api/foodtrucks` - 푸드트럭 목록
- `GET /api/foodtrucks/:id` - 푸드트럭 상세
- `GET /api/foodtrucks/:id/menu` - 메뉴 조회
- `POST /api/foodtrucks/:id/menu` - 메뉴 등록 (운영자)
- `PUT /api/foodtrucks/:id/menu/:menuId` - 메뉴 수정 (운영자)
- `DELETE /api/foodtrucks/:id/menu/:menuId` - 메뉴 삭제 (운영자)

**주문**

- `GET /api/orders` - 내 주문 목록
- `GET /api/orders/:id` - 주문 상세
- `POST /api/orders` - 주문 생성
- `PUT /api/orders/:id/status` - 주문 상태 변경 (운영자)
- `POST /api/orders/:id/cancel` - 주문 취소

**관리자**

- `GET /api/admin/stats` - 통계 대시보드
- `GET /api/admin/users` - 사용자 목록
  - **`?studentVerified=true`** - 📌 NEW: 학생 인증 필터
- `PUT /api/admin/users/:id/role` - 역할 변경
- **`PUT /api/admin/users/:id/verify-student`** - 📌 NEW: 학생 인증 수동 승인
- `GET /api/admin/events` - 전체 행사 관리
- `POST /api/admin/notifications` - 푸시 알림 발송

---

## 8. 우선순위 및 로드맵

### 8.2 기능별 우선순위

#### P0 (필수 - MVP)

- 회원가입/로그인 (일반 이메일)
- 행사 등록 및 조회
- **학생 전용 행사 표시**
- 예약 시스템
- QR 체크인
- 푸드트럭 주문 및 결제
- 지도 기본 기능

#### P1 (중요 - Phase 2)

- **학생 인증 시스템**
- 실시간 알림
- 관리자 대시보드
- 통계 리포트
- 푸드트럭 운영자 대시보드

#### P2 (향상 - Phase 3+)

- 소셜 로그인 (카카오, 구글)
- **전화번호 인증**
- 사용자 리뷰 시스템
- 이벤트 공유 기능
- 다크 모드
- 다국어 지원

#### P3 (부가 - 미래)

- AI 추천 시스템
- 채팅 기능
- 포인트/리워드 시스템
- 모바일 앱 (React Native)

---

**문서 끝**

_이 문서는 PKNU-Fest 프로젝트의 제품 요구사항을 정의한 것으로, 프로젝트 진행 중 필요에 따라 업데이트될 수 있습니다._
